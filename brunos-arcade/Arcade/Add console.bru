meta {
  name: Add console
  type: http
  seq: 2
}

post {
  url: {{BaseURL}}/Arcade/AddConsole
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

body:json {
  {
    "Name": "SNES"
  }
}

settings {
  encodeUrl: true
}

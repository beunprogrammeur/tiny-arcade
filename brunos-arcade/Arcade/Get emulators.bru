meta {
  name: Get emulators
  type: http
  seq: 5
}

get {
  url: {{BaseURL}}/Arcade/GetEmulators
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}

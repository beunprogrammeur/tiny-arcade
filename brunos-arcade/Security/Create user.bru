meta {
  name: Create user
  type: http
  seq: 5
}

post {
  url: {{BaseURL}}/Security/CreateUser
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

body:json {
  {
    "UserName": "TestUser",
    "Password": "TestPassword"
  }
}

settings {
  encodeUrl: true
}

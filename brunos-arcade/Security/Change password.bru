meta {
  name: Change password
  type: http
  seq: 4
}

post {
  url: {{BaseURL}}/Security/ChangePassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

body:json {
  {
    "UserName": "TestUser",
    "Password": "newTestPassword"
  }
}

settings {
  encodeUrl: true
}

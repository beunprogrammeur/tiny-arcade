meta {
  name: Am I logged in?
  type: http
  seq: 2
}

get {
  url: {{BaseURL}}/Security/IsLoggedIn
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

settings {
  encodeUrl: true
}
